<%-include('../layout/header') %>
  <%- include('../layout/navBar') %>

  <!-- ----------------- Notification section-----------------  -->
 <% if(notification.status === 'error') {%>
  <script>notificationMessage("<%=notification.message%>")</script>
 <%  } else if(notification.status=== 'success'){%>
  <script>successMessage("<%=notification.message%>")</script>
 <%  }%>
<link rel="stylesheet" href="../../user/order-style.css">
 <div class="container-fluid pt-5 ">
    <div class="row">
        <div class="col-xl-8">
            <!-- Active Products Section -->
            <h5 class="mb-3">Active Products</h5>
            
             <% let total =0;
            let totalQuantity = 0;
            if(activeProducts && activeProducts.length > 0) {
                activeProducts.forEach(product => { 
                    total+=(product.product_price * product.product_quantity)
                    totalQuantity += product.product_quantity
                    %> 
           
            <div class="card border shadow-none mb-3">
                <div class="card-body">

                    <div class="d-flex align-items-center border-bottom pb-3">
                        <div>
                            <div class="me-4">
                            <img src="../../<%=product.product_image %>" alt="" class="avatar-lg rounded order-product-img">
                        </div>
                        </div>
                        <div class="flex-grow-1 align-self-center overflow-hidden">
                            
                            <div>
                                <h5 class="text-truncate font-size-18"><a href="#" class=""><%=product.product_name  %></a></h5>
                                <p class="text-muted mb-0">
                                    <i class="bx bxs-star text-warning"></i>
                                    <i class="bx bxs-star text-warning"></i>
                                    <i class="bx bxs-star text-warning"></i>
                                    <i class="bx bxs-star text-warning"></i>
                                    <i class="bx bxs-star-half text-warning"></i>
                                </p>
                                <p class="mb-0 mt-1">Category : <span class="fw-medium color"><%=product.product_category %></span></p>
                                <p>
                                </div>
                                
                        </div>
                        
                    </div>

                    <div>
                        <div class="row">
                            <div class="col-md-4">
                                <div class="mt-3">
                                    <p class="text-muted mb-2">Price</p>
                                    <h5 class="mb-0 mt-2"><span class="text-muted me-2 color">₹<%= product.product_price %> </span></h5>
                                </div>
                            </div>
                            <div class="col-md-5">
                                <div class="mt-3">
                                    <p class="text-muted mb-2">Quantity</p>
                                    <div class="d-inline-flex">
                                        <select class="form-select w-xl" disabled>
                                            <option disabled selected><%= product.product_quantity %></option>
                                            
                                        </select>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="mt-3">
                                    <p class="text-muted mb-2">Total</p>
                                    <h5 class="color">₹<%= product.product_price * product.product_quantity %></h5>
                                    <% if(productData.orderStatus!=='Delivered' && productData.orderStatus!=='Returned' && productData.orderStatus!=='Cancelled' && product.product_status!=='Cancelled'){ %>
                                        <a href="/cancel-order-item/<%= String(productData._id) %>/<%= String(product.product_id) %>" class="btn btn-link p-0" onclick="return confirm('Cancel this item?');">Cancel this item</a>
                                        
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
            <% }); %>
            <% } else { %>
                <div class="alert alert-info">No active products in this order.</div>
            <% } %>
            
            <!-- Cancelled Products Section -->
            <% if(cancelledProducts && cancelledProducts.length > 0) {
                console.log("cancelledProducts", cancelledProducts)

                
            %> 
                <h5 class="mb-3 mt-4" style="color: #dc3545;">Cancelled Products</h5>
                
                cancelledProducts.forEach(product => { %>
                    <div class="card border shadow-none mb-3" style="opacity: 0.7; border-left: 4px solid #dc3545;">
                        <div class="card-body">
                            <div class="d-flex align-items-center border-bottom pb-3">
                                <div>
                                    <div class="me-4">
                                    <img src="../../<%=product.product_image %>" alt="" class="avatar-lg rounded order-product-img">
                                </div>
                                </div>
                                <div class="flex-grow-1 align-self-center overflow-hidden">
                                    <div>
                                        <h5 class="text-truncate font-size-16"><%= product.product_name %></h5>
                                        <p class="text-muted mb-0">
                                            <span class="me-2">Rating: <span class="text-warning">★★★★★</span></span>
                                        </p>
                                        <p class="text-muted mb-0">
                                            <span class="me-2">Category: <span class="fw-medium"><%= product.product_category %></span></span>
                                        </p>
                                        <p class="text-muted mb-0">
                                            <span class="me-2">Payment: <span class="fw-medium"><%= productData.paymentMethod %></span></span>
                                        </p>
                                    </div>
                                </div>
                                <div class="flex-shrink-0">
                                    <div>
                                        <h5 class="font-size-16 text-muted mb-0">₹<%= product.product_price %></h5>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">
                                    <div>
                                        <p class="text-muted mb-2">Quantity</p>
                                        <div class="input-group input-group-sm" style="width: 100px;">
                                            <input type="text" class="form-control" value="<%= product.product_quantity %>" readonly>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="mt-3">
                                        <p class="text-muted mb-2">Total</p>
                                        <h5 class="color">₹<%= product.product_price * product.product_quantity %></h5>
                                        <div style="font-size: 12px; color: #dc3545; font-weight: bold;">
                                            CANCELLED
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } %>
            
        </div>

        <div class="col-xl-4">
            
            <div class="mt-5 mt-lg-0">
                <div class="card border shadow-none">
                    <div class="card-header bg-transparent border-bottom py-3 px-4">
                        <h5 class="font-size-16 mb-0">Order Summary <span class="float-end">#<%=productData.order_id %></span></h5>
                    </div>
                    <div class="card-body p-4 pt-2">

                        <div class="table-responsive">
                            <table class="table mb-0">
                                <tbody>
                                    <tr>
                                        <td>Sub Total</td>
                                        <td class="text-end">₹<%= total.toFixed(2) %></td>
                                    </tr>
                                    <tr>
                                        <% const GST = Math.round((18 / 100) * total * 100) / 100; %>
                                        <td>GST</td>
                                        <td class="text-end">₹<%=GST %></td>
                                    </tr>
                                    
                                    <tr>
                                        <td>Shipping Charge :</td>
                                        <td class="text-end">₹<%= total <= 1000 ? '25' : '0' %></td>
                                    </tr>
                                    <tr>
                                        <td>Total Quantity</td>
                                        <td class="text-end"><%= totalQuantity %></td>
                                    </tr>
                                    
                                    <tr class="bg-light">
                                        <th>Total :</th>
                                        <td class="text-end">
                                            <span class="fw-bold">
                                                ₹ <%= (total + GST + (total <= 1000 ? 25 : 0)).toFixed(2) %>
                                            </span>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                            <% if(productData.orderStatus ==='Payment Pending'){ %>
                <div class="flex mt-3" >
                  <a href="/cancel-pending-order/<%= productData._id %>" class="  border p-3 border-round" >Cancel Order</a>
                  <a href="#" class="border rounded p-3  btn-success" onclick="paynow('<%= productData._id%>')">Re-Payment</a>
                </div>
            <% } %>
                        </div>
                        <!-- end table-responsive -->
                    </div>
                </div>
            </div>
            <% const address = productData?._doc?.address || {} %>
            <div class=" mt-lg-3 mt-3"><div class="card border shadow-none">
                    <div class="card-header bg-transparent border-bottom py-3 px-4">
                        <h6 class="font-size-16 mb-0">Shipping Address</h6>
                    </div>
                    <div class="single-address-div">
                          
                          <div class="address">
                            <% if (address && Object.keys(address).length > 0) { %>
                            <p><span>
                                <i class='bx bx-home'></i> <%=address.building || 'N/A' %>,
                              </span>
                              <span>
                                <%=address.street || 'N/A' %>
                              </span>
                              <span>
                                <%=address.city || 'N/A' %> ,
                              
                             <%=address.country || 'N/A' %> ,<%=address.pincode || 'N/A' %>,<%=address.phonenumber || 'N/A'
                                      %>
                                    </span>
                            <span>
                              <%=address.landmark || 'N/A' %>
                            </span></p>
                            <% } else { %>
                            <p class="text-muted">No shipping address available for this order.</p>
                            <% } %>
                          </div>
                        </div>
                    </div>
        </div>
    </div>
    <!-- end row -->
    
</div>

<script>
        document.addEventListener("DOMContentLoaded", function() {
            const progressBar = document.getElementById("progress-bar");
            const steps = document.querySelectorAll(".progress-step");

            // Define the steps to animate
            const completedSteps = 3; // Number of completed steps, adjust this value as needed

            steps.forEach((step, index) => {
                if (index < completedSteps) {
                    step.classList.remove("incomplete");
                } else {
                    step.classList.add("incomplete");
                }
            });

            const progressWidth = (completedSteps / (steps.length - 1)) * 100;
            progressBar.style.width = `${progressWidth}%`;
        });
    </script>